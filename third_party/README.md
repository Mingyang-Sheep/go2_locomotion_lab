# Third-party boundary

No upstream source is vendored in Phase A. The project consumes the server's clean
Isaac Lab checkout and official `isaaclab_assets` Go2 configuration through editable
package installs. Future HIM references belong here as pinned, documented dependencies;
their code must not be mixed into Competition PPO.

